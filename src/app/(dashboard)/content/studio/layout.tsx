import { ContentIntelligenceShell } from "@/components/content-intelligence/content-intelligence-shell";

export default function ContentStudioLayout({ children }: { children: React.ReactNode }) {
  return <ContentIntelligenceShell>{children}</ContentIntelligenceShell>;
}
